{
  "name": "stonebaby",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix client",
    "build": "webpack --mode production v",
    "start": "node frontend.js --prefix client"
  }
}